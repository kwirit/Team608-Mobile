package com.example.scratchinterpretermobile.Model

class Interpreter {
    // хранит скрипт где каждый элемент скрипта - блок-инструкция

    fun run(): Int {
        // итеррируемся по скрипту и запускаем .run() для каждого блока-инструкции
        return 0
    }
}